---
scope: 整个电子木鱼产品：嵌入式层 Embedded + 软件层 cloud/backend、cloud/frontend
purpose: build-substrate — 供下游 bmad-spec / epic / story / build 保持一致的 invariants 契约
altitude: feature
updated: 2026-09-08T17:53
---

- (direction) 产品分两层：嵌入式层 Embedded(固件+硬件行为) 与 软件层 Software(cloud/backend 后端 + cloud/frontend 微信小程序)；单一 ARCHITECTURE-SPINE 覆盖两层与跨层同步契约。
- (override) 后端持久化 = JSON 文件、零数据库；覆盖 PRD FR-B-009 与旧 architecture.md §8.3 的『单文件 SQLite』决定；不部署任何数据库服务。
- (direction) 软件层骨架沿用 miaowu：Spring Boot 3.3.7 + Java 17、/api/v1 + {code,message,data} 信封 + 微信登录/JWT、uni-app(Vue3+TS+Vite) 仅微信小程序、单 VITE_API_BASE_URL；JSON 原子落盘零库做法参照 legbot_watch/cloud。
- (direction) 嵌入式层沿用 legbot_watch 范式：分层 BSP/Driver + FreeRTOS 服务任务事件驱动 + 单一产品事实源；复用其 CO5300/CST9217/CW2015/ES8311(QSPI/I2C) BSP 与 managed_components 基线；不沿用其引脚常量、BLE/外骨骼业务。
- (constraint) 输入权威顺序：09-08 简报/附录 > PRD(FR-E/B/F) > 旧 architecture.md；冲突以 09-08 简报/附录与本轮用户决策为准。
- (decision) 正式 spine 定稿后删除根目录 architecture.md；其硬件/同步细节（引脚、电源、USB/JTAG、字段、门禁）先并入 spine seed/AD，防信息丢失；PRD §0 等活跃引用改指新 spine。
- (assumption) backend→小程序推送沿用 PRD 既定 WebSocket；miaowu 无此先例且小程序需合法 wss 域名 —— 开放项，可退化短轮询，finalize 前与用户确认。
- (event) bmad-architecture create 运行开始（Fast path；用户已批准计划）
- (version) ESP-IDF：现行 major v6.0.x(2026-03)；v5.5 线维护至 2026-07 后转 maintenance，最新 5.5.5。嵌入式沿用 legbot 锁档 ≥5.5.4,<5.6.0 以复用其 BSP/managed_components(CO5300/CST9217/ES8311/CW2015)；原型期 5.5.x，升级 v6.0 列为 deferred。2026-09-08 web 复核。
- (version) Spring Boot 3.3.x OSS 支持已于 2025-06-30 EOL（末补丁 3.3.13）。沿用 miaowu pom 3.3.7 作为配置基线（个人单设备原型，安全窗口非关注点）；升级到受支持维护线列为 deferred。2026-09-08 web 复核。
- (decision) spine 内嵌 addendum §3.1 引脚表、I²C/电源/USB/启动绑带与同步字段等板级 seed，确保删除根 architecture.md 后信息不丢失。
- (decision) backend→小程序实时通道：保留 WebSocket（用户 finalize 确认）。Spring Boot 原生 WebSocket + 小程序 wx.connectSocket；断线经查询补齐；需作者自备合法 wss 域名。AD-17 从『待定』改为已定。
- (decision) 后端 JSON 持久化 schema 不在 spine 冻结（用户确认）；spine 只锁不变量：零库 + 原子写(tmp+rename) + 单实例串行 + 重启可恢复，具体 schema/文件粒度下沉 docs/contracts 与 bmad-spec/实现阶段。
- (event) 5 个对账/评审子代理完成，报告落 reviews/：product 对账 4 项补强、arch-parity must-fill、tech-reality 出处与 3 处版本修正、adversarial 4 个对账缝隙、rubric CRIT-1 轮次归属洞。lint_spine 0 finding。
- (decision) 按评审回写 spine：新增 round_id（单调轮次标识）进同步字段并随包上报；新增 AD-19 经文消费/展示语义（一敲一字、标点随附不耗敲、不预览未来、从头开始/退出）；收紧 AD-2/3/5/6/17 对账闭环——backend 只校验确认不按长度盲推游标、同步响应回传确认后 round_state/cursor、命令生效靠已应用修订高水位（不依赖单次 ACK）、断线补齐以查询快照水位 S 为基准、设备『今日』非权威同时区；AD-1 补熄屏首触只唤醒；AD-4 补单一文本源构建期一致。
- (decision) spine 增补『版本与依据核验出处』表(2026-09-08，含 Espressif 硬件依据链接随迁)；Deferred 后端升级目标改为现行受支持 OSS 线 4.0.x/4.1.x；记录 LVGL8.4 上游 2025-03 停更、Java17 建议 Temurin/Corretto。
- (event) spine 定稿 lint 0 finding、AD 1..19；status 转 final 待收尾。删除旧 architecture.md 前 arch-parity 已确认安全（must-fill 已并入）。
- (event) spine finalized：status=final，AD-1..19，lint 0 finding。
- (event) 已删除根目录 architecture.md（内容并入 spine 并经 arch-parity 对账确认安全）；PRD §0/§11 与 09-08 附录的活跃引用已改指新 spine。
