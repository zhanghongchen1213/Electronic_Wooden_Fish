---
scope: Electronic_Wooden_Fish 软件环境配置、本地脚本与云端后端交付契约
purpose: 更新现有架构主干并固定 backend/frontend 配置与 env-scripts 的跨环境不变量
altitude: feature
updated: 2026-09-22T01:19
---

- (constraint) 继承现有 AD-1 至 AD-19、JSON 零数据库、backend 权威同步、uni-app 微信小程序和现有硬件事实源；本次只扩展环境/交付边界，不重编号既有 AD。
- (decision) backend 配置固定落在 cloud/backend/src/main/resources/application.yml（生产默认）与 application-local.yml（local profile）；配置不迁入 miaowu 的 PostgreSQL、Flyway、Jeepay、OBS 或支付回调。
- (decision) frontend 只维护 cloud/frontend/.env（生产）与 cloud/frontend/.env.development（本地）；本地默认 API 为 http://localhost:9218/api/v1，不生成或依赖 .env.development.local。
- (decision) start-local-test-backend.sh 与 stop.sh 只管理 EWF cloud/backend 的 local profile、9218 端口、logs/PID 和 saas.jar；移除 cloudflared、公网隧道、支付回调和 miaowu 专用匹配。
- (decision) build-prod-backend.sh 使用通用 Linux JDK17/Maven shell 契约，识别 cloud/backend/pom.xml，执行 mvn -B -U clean package -DskipTests，并校验 cloud/backend/target/saas.jar。
- (decision) upload-backend-jars.sh 使用 EWF Jar 路径，沿用当前 SSH 主机、root 用户与 /www/wwwroot/woodenfish；SSH 密码按用户要求直接写入脚本，不读取 SSH_PASSWORD 环境变量。
- (assumption) 生产 API 域名与微信 AppID 暂不复制 miaowu 当前值；本轮只冻结配置字段与替换位置，真实值在部署前补齐。
- (assumption) 按用户确认，application\*.yml 中 JWT/微信等应用密钥可明文固定；在 Deferred 中记录公开仓库或正式生产前的安全复核门禁。
- (event) 开始更新现有 ARCHITECTURE-SPINE.md、四个 env-scripts 及 backend/frontend 环境配置。
- (decision) 补强 AD-20：同一环境的 backend wechat.mini.app-id 与 frontend VITE_WX_APPID 必须一致，避免配置文件分裂。
- (decision) 为避免构建/上传之间误传同名 Jar，build-prod-backend.sh 生成 saas.jar.sha256，upload-backend-jars.sh 上传前校验并同时上传 sidecar。
- (decision) 端口契约收紧为固定 9218；start/stop、application.yml、application-local.yml 和 .env.development 必须一致，不允许通过环境变量漂移。
- (decision) VITE_API_BASE_URL 固定表示包含 /api/v1 的 REST base；WebSocket origin/path 由同步契约派生，不在 env 中重复拼接 REST 前缀。
- (event) Reviewer Gate 发现的端口漂移与产物来源风险已修正；Huawei Cloud 构建仍受 cloud/backend 尚无 pom.xml 的绿地状态限制。
- (decision) 收紧脚本根目录解析：显式 CI 根目录优先，其次脚本所在 checkout，再回退当前工作目录，避免从另一份 checkout 误取同名 Jar。
- (decision) 生产 与 application.yml 的 EWF 域名/AppID 占位值属于发布前门禁；正式 CI 接入时必须 fail-closed 检查，禁止占位值上线。
- (event) 对抗性复审达到 Conditional Pass；剩余明文凭据、host-key、正式制品签名和远端运维属于已记录 Deferred/原型风险。
- (decision) 更正上一条记录：生产 .env 与 application.yml 的 EWF 域名/AppID 占位值属于发布前门禁；正式 CI 接入时必须 fail-closed 检查，禁止占位值上线。
- (event) spine finalized: environment/configuration and delivery scripts updated; lint and scoped checks passed; backend Maven build remains blocked until cloud/backend/pom.xml exists.
- (decision) build-prod-backend.sh 与 start-local-test-backend.sh 在实际构建路径强制 JDK 17；执行 Maven 构建时强制 Maven 3.9.x，避免只在注释中声明版本。
- (event) Reviewer 版本复审的 P1 版本门禁已修正；Spring Boot/pom/finalName 仍待 backend 绿地骨架落地后验证。
- (event) post-review fixes finalized: JDK17 and Maven3.9.x checks added; version/pom absence explicitly deferred to backend scaffold.
- (decision) 更正早期评审草稿中的 9218：当前并已冻结的 EWF 本地端口统一为 9218；所有涉及 9218 的历史条目均由本条 supersede，不再作为实施依据。
- (event) 版本复审已基于当前工作树更新：JDK17/Maven3.9.x 与 local profile 门禁通过；剩余明文 SSH/host-key、EOL 基线、backend pom/finalName 属后续实现门禁。
